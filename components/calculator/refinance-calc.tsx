'use client';
// STUB — implementación real en Task 11 (lib/calc/refinance.ts ya existe y está testeado;
// falta la UI). Renderiza null a propósito: solo existe para que CalcTabs compile en Task 9.

export type RefinanceCalcTexts = Record<string, never>;

export function RefinanceCalc(_props: { locale: string; texts: RefinanceCalcTexts }) {
  return null;
}
