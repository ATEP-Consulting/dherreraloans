'use client';
// STUB — implementación real en Task 10 (lib/calc/affordability.ts ya existe y está testeado;
// falta la UI). Renderiza null a propósito: solo existe para que CalcTabs compile en Task 9.

export type AffordabilityCalcTexts = Record<string, never>;

export function AffordabilityCalc(props: { locale: string; texts: AffordabilityCalcTexts }) {
  void props;
  return null;
}
